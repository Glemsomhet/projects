name: coin_flip
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.2
  vibration: ^3.1.3
  simple_animations: ^5.1.0
  drift: 
  timeago: ^3.6.1

  # PostgreSQL için yeni bağımlılıklar
  drift_postgres:
  postgres:
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  drift_dev:
  build_runner:

flutter:
  uses-material-design: true
